package Project2;


import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class ScoreInfoService{
    private ScoreData scoreData;

    private static class ScoreData {
    }
    private static List<ScoreData> scoreDataList = new ArrayList<>();

    public void addScoreData(ScoreInfo scoreInfo){
        scoreDataList.add(scoreData);
    }

   // public static void getNumberOfStudents(){
       // return (int)scoreDataList.stream()
     // Student NumberOfStudents = scoreDataList.stream()
             // .map(ScoreData :: )
            //  .distinct()
             // .count();
    }

    
//}
